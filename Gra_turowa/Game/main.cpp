#include "hpp/controler.hpp"

/**
 * Projekt nie posiada opcji wczytywania i zapisywania do pliku. Pokemony odnawiaja hp oraz mają zdejmowane działanie wszystkich umiejętności po zakońćzonej rundzie.
 */


int main() {
    srand(time(NULL));
    controler::welcome();

    return 0;
}
